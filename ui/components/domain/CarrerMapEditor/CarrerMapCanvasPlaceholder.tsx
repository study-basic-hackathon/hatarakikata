"use client"

export default function CarrerMapCanvasPlaceholder() {
  return (
    <div className="flex items-center justify-center w-full h-full text-foreground/40">
      生年月日を設定するとキャリアマップが表示されます
    </div>
  )
}
